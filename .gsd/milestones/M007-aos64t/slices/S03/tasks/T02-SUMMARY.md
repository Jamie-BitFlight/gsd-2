---
id: T02
parent: S03
milestone: M007-aos64t
provides:
  - Verification that all factcheck tests pass together without regression
  - Confirmed structural validity of the durable validation report
key_files:
  - .gsd/milestones/M007-aos64t/M007-VALIDATION-REPORT.json
key_decisions:
  - Tests require `npx tsx --test` instead of `node --test` due to transitive .js imports in .ts files
patterns_established:
  - Full proof suite runs all three test files together to confirm no regressions
observability_surfaces:
  - M007-VALIDATION-REPORT.json — machine-parseable milestone closeout evidence
duration: 10 minutes
verification_result: passed
completed_at: 2026-03-18T19:56:00Z
blocker_discovered: false
---

# T02: Validate report schema and run full proof suite

**Ran all three factcheck test files together (42 tests) and verified the validation report schema is structurally complete with all required fields.**

## What Happened

Executed the full proof suite by running all three factcheck test files together with `npx tsx --test`. All 42 tests passed (3 final-audit, 30 fixture, 9 live). The validation report was already generated by T01 and was verified to have correct schema version, result status, and evidence fields.

The slice plan specified `node --test` but that command fails for the gsd extension tests because Node's `--experimental-strip-types` doesn't handle transitive `.js` imports within `.ts` files. The T01 summary already documented this — tests require `npx tsx --test` instead.

## Verification

1. **Full proof suite passes** — `npx tsx --test src/resources/extensions/gsd/tests/factcheck-runtime-fixture.test.ts src/resources/extensions/gsd/tests/factcheck-runtime-live.test.ts src/resources/extensions/gsd/tests/factcheck-final-audit.test.ts` — all 42 tests pass
2. **Validation report schema verified** — `node -e "..."` confirms schemaVersion=1, evidence.refutedCount>=1, evidence.rerouteTarget present, evidence.correctedValuePresent=true, result=PASS
3. **No regressions** — S01 fixture tests (30 tests) and S02 live tests (9 tests) pass unchanged

## Verification Evidence

| # | Command | Exit Code | Verdict | Duration |
|---|---------|-----------|---------|----------|
| 1 | `npx tsx --test factcheck-runtime-fixture.test.ts factcheck-runtime-live.test.ts factcheck-final-audit.test.ts` | 0 | ✅ pass | 995ms |
| 2 | `node -e "const r=JSON.parse(readFileSync(...)); assert(r.schemaVersion===1); ..."` | 0 | ✅ pass | <50ms |
| 3 | `npx tsx --test factcheck-runtime-live.test.ts` | 0 | ✅ pass | 742ms |

## Diagnostics

- **Validation report location**: `.gsd/milestones/M007-aos64t/M007-VALIDATION-REPORT.json`
- **Schema version**: 1 (stable)
- **Key fields for inspection**: `evidence.refutedCount=1`, `evidence.rerouteTarget="plan-slice"`, `evidence.correctedValuePresent=true`, `result="PASS"`
- **Run proof suite**: `npx tsx --test src/resources/extensions/gsd/tests/factcheck-*.test.ts`

## Deviations

- **Test execution method**: Slice plan specified `node --test` but tests require `npx tsx --test` due to module resolution limitations. This was already documented in T01's summary.

## Known Issues

None. All must-haves satisfied. The `npm run test` command runs the full project test suite which may have unrelated failures, but all factcheck-specific tests pass.

## Files Created/Modified

- `.gsd/milestones/M007-aos64t/M007-VALIDATION-REPORT.json` — Verified structurally valid (created by T01, verified by T02)
